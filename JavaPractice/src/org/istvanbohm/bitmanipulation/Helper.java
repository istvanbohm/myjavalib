package org.istvanbohm.bitmanipulation;

public class Helper {

	/**
	 * Prints the number in a binary format
	 * @param num the number
	 */
	public static void printNum(int num) {
		System.out.println("(" + num + ") " + Integer.toBinaryString(num));
	}
	
}
